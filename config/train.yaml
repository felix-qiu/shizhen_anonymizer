model:
  name: yolo11s.pt
  version: v1

train:
  epochs: 100
  imgsz: 640
  batch: 16
  device: auto

dataset:
  path: datasets/ultrasound_roi
  yaml: datasets/ultrasound_roi/dataset.yaml

output:
  dir: runs
  name: train

export:
  dir: models/trained
